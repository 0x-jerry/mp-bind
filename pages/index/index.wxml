<!-- index.wxml -->
<view class="container">
  <view>{{count}}</view>
  <view>{{motto}}</view>
  <input bindinput="inputHelper" data-name="count" />
  <button bind:tap="bindViewTap"></button>
</view>
