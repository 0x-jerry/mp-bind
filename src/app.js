// app.js

import { setConfig } from './core/index'
setConfig({ debug: true })

import { setConfig } from './core/index';
setConfig({ debug: true });

App({
  onLaunch() {},
  globalData: {},
});

import './test';
